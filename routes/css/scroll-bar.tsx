import { define } from "tools/utils.ts";

export default define.page(function ScrollBar() {
  return (
    <main>
      <h1>Scroll Bar</h1>
      <article>
        这是很长的一段内容
      </article>
    </main>
  );
});
